# --- Telegram Settings ---
# Your bot token, obtained from @BotFather
MONITOR_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN

# Chat ID (or your personal account ID) where the bot will send notifications.
# You can find it using bots like @userinfobot
MONITOR_ADMIN_CHAT_ID=YOUR_TELEGRAM_CHAT_ID

# --- AI Settings (OpenRouter) ---
# API key from OpenRouter (https://openrouter.ai/keys)
OPENROUTER_API_KEY=YOUR_OPENROUTER_API_KEY

# Model for summary generation (default is openai/gpt-4o-mini)
# Good options:
# - anthropic/claude-haiku-4.5 (fast and cheap)
# - openai/gpt-4o-mini (balanced)
# - google/gemini-2.0-flash-001 (very fast)
OPENROUTER_MODEL=anthropic/claude-haiku-4.5

# Language in which AI summaries will be generated (default is "русском языке")
# Examples: "english", "french", "spanish", "german"
SUMMARY_LANGUAGE=english

# Output token limit for AI responses (default is 1000)
# Increase if responses are getting truncated.
OPENROUTER_MAX_TOKENS=2000

# Input character limit for release notes sent to AI (default is 4000)
# Increase if your release notes are very long and you want more context to be processed.
OPENROUTER_MAX_INPUT_LENGTH=4000

# --- GitHub Settings ---
# Personal Access Token (Classic) to increase API rate limits.
# Create here: https://github.com/settings/tokens (public_repo scope or no scopes for public repos is sufficient)
# Without a token, the limit is 60 requests per hour; with a token, it's 5000.
GITHUB_TOKEN=YOUR_GITHUB_TOKEN
